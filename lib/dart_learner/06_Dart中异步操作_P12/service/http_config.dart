/// HTTP Configuration
class HttpConfig {
  // Base URL for the API
  static const baseUrl = "https://httpbin.org";

  // Connect timeout for the API calls
  static const connectTimeout = 10;

  // Receive timeout for the API calls
  static const receiveTimeout = 10;
}
